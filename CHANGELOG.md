# Changelog

## 0.3.3 — Unreleased

- 6pm PlugWalk DNA inventory of today's Auto-off levers on official Classroom + loft (no bpy, no GPU, no render, no zip). Classroom from download.blender.org/demo/test/classroom.zip (zip 70279690, extracted classroom.blend 32045332 bytes, sha256 5c526ea3f280566e80253673c9955640527cd0f247ea41b1742620b5bc39f7a4, BLENDER-v279, not git-added). loft from download.blender.org/demo/cycles/loft.blend (561122088 bytes, sha256 96d31b9c0df55592bde4a82d875e05acece67201d5df9cac40ef9d164a7c1840, BLENDER-v292, not git-added). BMW27 re-ran (3148419 bytes, sha256 98306c8affc0a4513e6998a1c2813a66083264e7e28b5e853975555b55c308e4). FIRED all four = 0 on all three files. UNKNOWN is not 0: loft/BMW27 sample_clamp_indirect key missing (hasattr fire=0); Classroom clamp PRESENT 3.0 (user value stays); Classroom 4 noded lights Strength UNKNOWN_OR_LIVE not proven 0; loft world Strength LINKED UNKNOWN, sampling_method UNKNOWN (not NONE), spatial TEX_ENVIRONMENT. Classroom world Strength proven 0.0 already sampling_method=NONE no spatial. No energy-0 lights (Classroom 4x energy 1.0; loft 10/10/100/200). No 5th lever (docs/research/NEXT-LEVER.md). Reader tools/_inventory_today_levers_dna.py. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- BMW27 DNA inventory for today's Auto-off levers (no bpy): official `BMW27.blend` (`BLENDER-v273`, 3148419 bytes, sha256 `98306c8affc0a4513e6998a1c2813a66083264e7e28b5e853975555b55c308e4`, not git-added). `sample_clamp_indirect` key **UNKNOWN** (2.73 Scene `cycles` IDP has no socket; missing ≠ 0). Lights **0** (`LA` blocks 0, object type 10 = 0) so ZERO_ENERGY_LIGHT=0 ZERO_SHADER_LIGHT=0. World `use_nodes=1`, Background Strength **proven 1.0** (not 0), Color is RGB not env tex, `sampling_method` key missing → ZERO_WORLD_BG=0. Classroom/loft not on the box; those counts not invented. No 5th lever this hour (`docs/research/NEXT-LEVER.md`). Reader `tools/_inventory_today_levers_dna.py`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- ZERO_WORLD_BG: `world.cycles.sampling_method` NONE when a noded World Surface is proven-zero (Background/Emission Strength ~0 or unlinked black Color) **and** a spatial texture still feeds Color, so WORLD_MIS_NONE (solid-only) does not fire. Classifier is world DNA + the World Output Surface graph (`use_nodes` True, Strength proven 0 / Value-0 one hop, Color path has TEX_ENVIRONMENT / TEX_SKY / TEX_IMAGE / …, sampling_method != NONE, Volume unlinked, no portal lights, not linked, not animated, not HERO/KEEP) — same code for any .blend, never a file name. Complementary to WORLD_MIS_NONE (that Auto lever owns nodeless / no-texture trees). Cycles 4.5/5.1 `intern/cycles/scene/light.cpp` `test_enabled_lights`: `disable_mis = !(has_portal || shader->has_surface_spatial_varying)` — does **not** consult `emission_estimate`. `intern/cycles/scene/shader.cpp` `output_estimate_emission` still yields 0 on Strength 0 even when Color is linked; `intern/cycles/scene/svm.cpp` `generate_node` latches `has_surface_spatial_varying` from the connected env tex. `sync_background_light` then `device_update_background` / `shade_background_pixels` still builds the importance map. sampling_method NONE makes `sample_as_light` false (4.5 skips creating the background light without portals; 5.1 creates it with `use_mis` false and `is_enabled` false). Camera rays still evaluate the (black) world shader. Never unlinks. Never portals. Apply journals `cycles.sampling_method`; revert restores. `zero_world_bg_actions` is not called from `build_speed_plan` (tier 2, time_factor 1.0). Auto off until a measured pair. No time claim. Does not change Classroom 41% / loft 52%.
- ZERO_SHADER_LIGHT: `hide_render` on local Lights whose Cycles shader `emission_estimate` is proven 0 while RNA `Light.energy` is not. Classifier is object DNA + the Light Output Surface graph (`use_nodes` True, Strength proven 0 / unlinked black Color / unconnected Surface, hide_render False, not linked, not animated, not HERO/KEEP, not `Light.cycles.is_portal`, energy != 0) — same code for any .blend, never a file name. Complementary to ZERO_ENERGY_LIGHT (that lever owns RNA energy == 0). Cycles 4.5 `intern/cycles/blender/shader.cpp` `sync_lights`: node tree only when `use_nodes`; otherwise Emission Strength 1. `intern/cycles/blender/light.cpp` `sync_light` still writes `strength = color * energy * exp2(exposure)` with no node-tree read and no energy-0 early-out. `intern/cycles/scene/shader.cpp` `output_estimate_emission` then `intern/cycles/scene/light.cpp` `Light::has_contribution` returns false on `emission_estimate == 0` so the light tree skips them *after* they are Cycles objects (`object.cpp` lights skip camera cull). hide_render drops them from `DEG_OBJECT_ITER_FOR_RENDER_ENGINE`. Portals stay. GROUP / texture / Math / Light Path unproven. Never writes `Light.energy`. Never unlinks nodes. Apply journals `hide_render`; revert restores. `zero_shader_light_actions` is not called from `build_speed_plan` (tier 2, time_factor 1.0). Auto off until a measured pair. No time claim. Does not change Classroom 41% / loft 52%.
- ZERO_ENERGY_LIGHT: `hide_render` on local Lights whose RNA energy is 0. Classifier is object DNA only (type LIGHT, `Light.energy` == 0, hide_render False, not linked, not animated, not HERO/KEEP, not `Light.cycles.is_portal`) — same code for any .blend, never a file name. Cycles 4.5 `intern/cycles/blender/object.cpp` `sync_object`: lights skip camera cull and always sync when `show_lights`. `intern/cycles/blender/light.cpp` `sync_light` writes `strength = color * energy * exp2(exposure)` with no energy-0 early-out. `intern/cycles/scene/light.cpp` `Light::has_contribution` then returns false on `strength == zero_float3()` so the light tree skips them *after* they are Cycles objects. hide_render drops them from `DEG_OBJECT_ITER_FOR_RENDER_ENGINE`. Portals stay (world MIS rectangles, do not emit). Never writes `Light.energy`. Apply journals `hide_render`; revert restores. `zero_energy_light_actions` is not called from `build_speed_plan` (tier 2, time_factor 1.0). Auto off until a measured pair. No time claim. Does not change Classroom 41% / loft 52%.
- CLAMP_INDIRECT: when `sample_clamp_indirect` is left at 0 (disabled) write Cycles factory 10. Classifier is integrator DNA only (`scene.cycles.sample_clamp_indirect` == 0, CYCLES engine, hasattr-gated) — same code for any .blend. Cycles 4.5/5.1 `intern/cycles/scene/integrator.cpp`: socket default is 10.0f; kernel maps 0 → `FLT_MAX` (no clamp) else `value * 3.0f`. Direct clamp stays 0 (Cycles socket default 0.0f; never write `sample_clamp_direct`). `APPLY_PERCEPTUAL_PATHS` MODE_MIN 5.0 only *lowers* a high clamp and cannot enable a disabled one — that is the gap. User values already > 0 stay. Apply journals `cycles.sample_clamp_indirect`; revert restores. `clamp_indirect_actions` is not called from `build_speed_plan` (tier 2, time_factor 1.0). Auto off until a measured pair. No time claim. Does not change Classroom 41% / loft 52%.
- PRUNE_BUMP + PRUNE_BEVEL: Cycles 4.5 `BumpNode::constant_fold` (`intern/cycles/scene/shader_nodes.cpp`) only bypasses when Height is unlinked; Strength 0 is a TODO so the node stays, `get_feature()` returns `KERNEL_FEATURE_NODE_BUMP`, and `ShaderManager::get_graph_kernel_features` ORs that across every shader. `BevelNode` Radius 0 still latches `KERNEL_FEATURE_NODE_RAYTRACE`. Unlink the **consumer** Normal (typically Principled) so simplify drops the node. Never unlink Strength (default 1.0 would ENABLE bump) or Radius (default 0.05 would ENABLE bevel). Height-unlinked with Strength not proven-0 is already folded — no write. Displacement socket is already PRUNE_DISPLACE; skip Ambient Occlusion this pass. Apply is existing NODE_UNLINK. `dead_closure_prune_actions` not called from `build_speed_plan`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- PRUNE_TRANSMISSION + glass-skip honesty: `_principled_transmits` treated any linked Transmission Weight / Transmission as glass, so KEEP_GLASS skipped the whole material and blocked PRUNE_SSS / PRUNE_EMISSION / PRUNE_ALPHA on PBR dumps that wired Weight to Value 0 or a black map. Cycles 4.5 has no `has_surface_transmission` link-OR latch (Weight is `stack_assign` into NODE_CLOSURE_BSDF). Linked proven-zero (Value ~0, one hop, no Math/texture/GROUP) is not glass; the principled walk runs. Unlink that dead Weight (existing NODE_UNLINK, class `PRUNE_TRANSMISSION`). BSDF_GLASS / BSDF_REFRACTION still KEEP_GLASS. Unlinked default > 0.2 and unproven links still KEEP_GLASS. Apply exists, `dead_closure_prune_actions` not called from `build_speed_plan`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- PRUNE_SSS + PRUNE_EMISSION: Cycles 4.5 `has_surface_bssrdf` / `has_surface_emission` (`intern/cycles/scene/shader_nodes.cpp`) latch on a **link OR value**. Principled defaults: Subsurface Weight 0, Scale 0.1 (not zero), Emission Color white, Strength 0. A Weight link to constant 0 still ORs `KERNEL_FEATURE_SUBSURFACE` scene-wide; a Strength link to 0 + white Color still estimates `one_float3()` mesh light. `simplify_settings` does not disconnect constant-0 Weight. Unlink only proven-zero Weight / Strength (existing NODE_UNLINK). Never unlink Scale (would ENABLE SSS) or Emission Color. Image / Math / GROUP / unlinked-0 / real-weight skipped. Same reachable Principled walk as Alpha; glass / GROUP / linked / HERO still skip the material; opaque materials are not skipped. Apply exists, `dead_closure_prune_actions` not called from `build_speed_plan`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- UNUSED_SLOTS apply gate: classify still inventories every unique unused slot; apply pops only `extra_attrs` non-empty and not GROUP (UV / UV_TANGENT / VCOL / GENERATED). Empty extra_attrs (shader compiles, no extra attr union) and GROUP (unexpanded) stay. Inventory prints EXTRA_ATTR_APPLY_SLOTS + extra-attr shader list vs UNIQUE_UNUSED_SLOTS. loft DNA recount (no bpy): UNIQUE_UNUSED_SLOTS=21077 UNIQUE_UNUSED_SHADERS=45 EXTRA_ATTR_SLOTS=4865 EXTRA_ATTR_APPLY_SLOTS=4865 EXTRA_ATTR_SHADERS=15 on 891 meshes; extra_attrs tokens UV=4857 UV_TANGENT=26; GROUP extra=0 on unique unused. Apply would pop 4865 not 21077. `unused_slots_actions` still not called from `build_speed_plan`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- Packed-file magic-byte proof in `_image_has_no_alpha`: live bpy / DNA PackedFile can lack a useful filepath. Read first 8 bytes only (never the whole packed blob). JPEG SOI `ffd8` and BMP `BM` prove no-alpha; PNG signature is the alpha path (do not prune from magic). Empty packed data does not guess. Extension / file_format / channels checks stay. DNA inventory may set duck `file_format='JPEG'` from PackedFile magic; channels stay None; IGNORE is not guessed. Duck tests: packed JPEG → PRUNE_ALPHA; packed PNG → KEEP_REAL_CUTOUT; empty packed → no guess. loft Emission DNA (inventory only): EMISSION=5; MIX_OTHER=2 (`+luz_vidro1`, `Neon` Mix Fac=ColorRamp, not Transparent); UNCONNECTED=2 (`DA_Plywood.002`, `Material.015`); SURFACE_DIRECT=1 (`Material.008` Blackbody Emission Strength=10 on Cube.011/.012). MESH_EMIT_BACKFACE=0 PORTAL_MESH=0. No new general apply. Classroom MESH_EMIT_BACKFACE=2 stays; never `is_portal`. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- DNA Image duck-type: filepath is Image.filepath / 2.92 Image.name (1024-char path), never ID.name. channels stay None. alpha_mode maps 0/1/2/3 → STRAIGHT/PREMUL/CHANNEL_PACKED/IGNORE (unknown stays non-matching so IGNORE is not guessed). PRUNE_ALPHA inventory rows print alpha_src. UNUSED_SLOTS inventory prints unique-shader list + 12 example rows (`... N more`), not 21k. loft PRUNE_ALPHA=7 honesty: all 7 Principled Alpha images are packed JPEG `//../Blocos/moveis/camas/cama rustica/Maps/Baxter marilyn-Dirt 2.jpg` (id-name `Baxter marilyn-Dirt 2.jpg.001`). Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- loft DNA inventory (no bpy): official `loft.blend` on the user PC (561122088 bytes, `BLENDER-v292`, not git-added). Mix walk COMPLETE (UNKNOWN_FAC=0). **PRUNE_ALPHA=7** (JPEG / no-alpha Principled Alpha: Carpet Low Grey, Material.084 / .013 / .028, bed02, bed 13, Material.071). PRUNE_VOLUME=0 PRUNE_MIX_TRANSPARENT=0 PRUNE_DISPLACE=0. KEEP_GLASS=8 KEEP_REAL_CUTOUT=5 SKIP_GROUP=2. UNIQUE_UNUSED_SLOTS=21077 UNIQUE_UNUSED_SHADERS=45 EXTRA_ATTR_SLOTS=4865 on 891 Sketchup/kitchen meshes (inventory only). UNUSED_COLOR_ATTRS=0 PORTAL_MESH=0 MESH_EMIT_BACKFACE=0. L1 Auto gate is met on loft; Auto still off until a timed pair (those seven mats already paid OPAQUE_CUTOUT_SHADOWS in R6). No time claim. Does not change Classroom 41% / loft 52%.
- Pabellon Barcelona DNA inventory (no bpy): official `pabellon_barcelona_v1.scene_.zip` (`BLENDER-v266`, 24661092-byte zip / 8485449-byte `pavillon_barcelone_v1.2.blend`, not git-added). Scene `1- time: midday` Mix walk complete (UNKNOWN_FAC=0, material links intact). PRUNE_MIX_TRANSPARENT=0 PRUNE_DISPLACE=0 UNIQUE_UNUSED_SLOTS=0 UNUSED_COLOR_ATTRS=0 PORTAL_MESH=0 MESH_EMIT_BACKFACE=0 WORLD_PORTAL_CARD=0. 20 Mix Shader Fac proven (unlinked 0.05-0.5, none 0/1; rest linked LayerWeight/Image/Group). KEEP_GLASS=1 KEEP_REAL_CUTOUT=1 SKIP_GROUP=1. Third official DNA file besides Classroom + BMW27. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- Lone Monk DNA inventory (no bpy, optional extra): official `lone-monk_cycles_and_exposure-node_demo.blend` (`BLENDER-v292`, 36695778 bytes, not git-added). Mix walk complete. PRUNE_MIX_TRANSPARENT=0 PRUNE_DISPLACE=0 UNUSED_COLOR_ATTRS=0 PORTAL_MESH=0 MESH_EMIT_BACKFACE=0 WORLD_PORTAL_CARD=0. UNIQUE_UNUSED_SLOTS=1 (mesh `Mesh` slot 1 `wood - chair` on SM_table; SKIPPED_DUPLICATE_UNUSED=3). KEEP_REAL_CUTOUT=4 SKIP_GROUP=1. Inventory only, blend not written. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- BACKFACE_EMIT_OPAQUE no longer journals `use_backface_culling` (Cycles no-op). Auto off. 41%/52% unchanged.
- MESH_EMIT_SHADOW_SKIP inventory/apply (Auto off). Cycles-correct write: turn off object shadow ray visibility (`visible_shadow` / `cycles_visibility.shadow`, `intern/cycles/blender/object.cpp` `SD_OBJECT_SHADOW`) on MESH_EMIT_BACKFACE cards so shadow rays never hit the Transparent Mix. Camera/glossy/diffuse stay; emission still lights as a mesh light. Journal `SHADOW_VIS_OFF` `{object, prev}`. Not cull RNA. Not unlink Transparent (that is the quality-risk alternate: Cycles 4.5 does not sync `use_backface_culling`, F12 back becomes Emission). TRIM still keeps `visible_shadow` True. Planner hook `mesh_emit_shadow_skip_actions` (time_factor 1.0) is not called from `build_speed_plan`. Auto off until HDR-FLIP on Classroom and loft. Never `is_portal`. Never AREA convert. Never integrator RNA. No time claim. Official Classroom DNA (no bpy): MESH_EMIT_BACKFACE=2; 2.79 has no `visible_shadow` — Cycles ray vis is IDProperty `cycles_visibility.shadow`; hallWindow group empty (shadow key absent) → UNKNOWN; windows `shadow=0` already off; SHADOW_SKIP_OK not proven. BMW27: 0 emit cards. Does not change Classroom 41% / loft 52%.
- BACKFACE_EMIT_OPAQUE shadow-ray source note (blender-v4.5-release). Cycles does not sync `use_backface_culling` (`intern/cycles/blender/shader.cpp`). No `SHADER_BACKFACE_CULL` / `SD_USE_BACKFACE_CULLING`. Kernel `SD_BACKFACING` is post-hit (`shader_data.h`, camera + shadow + diffuse) and does not skip intersections — shadow rays are not culled. Unlink Transparent still drops `SD_HAS_TRANSPARENT_SHADOW` (`svm.cpp` `has_surface_transparent` on remaining closures; `shadow_all.h` opaque stop). Mix becomes Emission passthrough without cull; Cycles F12 back is Emission. Auto still off. No time claim. Does not change Classroom 41% / loft 52%.
- BACKFACE_EMIT_OPAQUE inventory/apply (Auto off). Unlink Transparent on MESH_EMIT_BACKFACE + journaled `use_backface_culling` so Cycles drops `SD_HAS_TRANSPARENT_SHADOW` (intern/cycles/scene/shader.cpp / kernel shadow retrace). hasattr-gated (Blender 4.1+ Cycles). Quality: outside the room the card disappears (cull) instead of transparent; window rebate / two-way views can change. Auto off until HDR-FLIP on Classroom and loft. Manual-first. Never `is_portal`. Never AREA convert. Never integrator RNA. No time claim. Does not change Classroom 41% / loft 52%.
- PORTAL_MESH role correction: official Classroom 2 hits (`dayLight_portal` on hallWindow / windows) are mesh *lights* with backface hide (`MESH_EMIT_BACKFACE`), not Cycles portal lights. Cycles `Light.cycles.is_portal` (`intern/cycles/scene/light.cpp`) does not emit — converting MESH_EMIT_BACKFACE to `is_portal` is forbidden (drops the lamp). Later convert (Manual, unbuilt) would be AREA light matching strength×area + hide_render mesh, not a portal. `WORLD_PORTAL_CARD` (Transparent+Backfacing without proven Emission) is a different class, still unbuilt. Auto off. No convert. No time claim. Does not change Classroom 41% / loft 52%.
- PORTAL_MESH inventory: hide_render=False MESH/CURVE whose used-face material is Mix(Transparent BSDF, Emission) with Fac linked to Geometry Backfacing. Skip HERO/EXCLUDE, linked ids, GROUP trees, glass/refraction/principled transmission, and objects that already are Cycles portal lights. Name is not how we detect. No convert, no light create. Planner hook `portal_mesh_actions` (time_factor 1.0) is not called from `build_speed_plan`. Auto off. No time claim. Official Classroom DNA (no bpy, `_mainScene`): PORTAL_MESH=2 (hallWindow / Box321.002 and windows / Plane, material dayLight_portal, Fac=Geometry.Backfacing proven). BMW27 DNA: PORTAL_MESH=0 (no Geometry node). Does not change Classroom 41% / loft 52%.
- Classroom DNA Mix inventory (no bpy): official `classroom.zip` (`BLENDER-v279`, 67 MB, not git-added). Local `_mainScene` Mix walk complete (UNKNOWN_FAC=0, material links intact). PRUNE_MIX_TRANSPARENT=0 PRUNE_DISPLACE=0 UNIQUE_UNUSED_SLOTS=0 UNUSED_COLOR_ATTRS=0. 14 Mix Shader Fac proven 0.01–0.15 (gloss mixes, not 0/1); only Transparent BSDF is `dayLight_portal` (Fac linked to Backfacing). KEEP_GLASS=1 SKIP_GROUP=1. Auto off. No time claim. Does not change Classroom 41% / loft 52%.
- BMW27 DNA inventory (no bpy) results: Mix-Transparent/Displace/unique-slots/color-attrs all 0. KEEP_GLASS=3. Auto off. Does not change Classroom 41% / loft 52%.
- PRUNE_DISPLACE: Material Output Displacement linked to a proven-zero constant (Value=0 / Combine XYZ 0 / Displacement Height+Scale 0). Unlink Displacement (existing NODE_UNLINK). Unconnected is already dead (no record). Texture / noise / GROUP / Math skipped. Apply exists, Auto off, no time claim. Official BMW27 DNA (no bpy): PRUNE_DISPLACE=0 (2 Displacement links are HueSat/Image, not proven-zero). Auto off, no time claim.
- UNUSED_COLOR_ATTRS: unique local meshes, skip linked / override / HERO / EXCLUDE / any modifier. Color attributes (color_attributes or vertex_colors) not named by a used-face Attribute / Vertex Color / Color Attribute node. UV maps and position/normal built-ins are never candidates. Inventory-only: apply would drop pixel values and revert cannot restore the layer data without a blob. Auto off, no time claim. Official BMW27 DNA (no bpy): UNUSED_COLOR_ATTRS=0 (no CD_MCOL/CD_MLOOPCOL). Auto off, no time claim.

- UNUSED_SLOTS: unique-shader gate. Duplicate unused slots of an already-used material are RNA noise (Cycles `get_used_shaders()` unions unique shaders) and are skipped. Unique unused materials stay prune keepers; extra_attrs tags UV / UV_TANGENT / VCOL / GENERATED / GROUP that used shaders on that mesh do not request. Official loft 2026-08-19: 891 meshes / 21k unused slots (pre-gate). Classroom: 0. Apply exists, Auto off, no time claim.
- DEAD_CLOSURE_PRUNE: inventory/classifier only. Mix Shader Fac proven 0/1 with unused Transparent BSDF is PRUNE_MIX_TRANSPARENT (unlink dead shader input; apply exists, Auto off). Official Classroom/loft: 0 PRUNE_ALPHA, 0 PRUNE_VOLUME. Official BMW27 DNA (no bpy): PRUNE_MIX_TRANSPARENT=0 (1 Transparent BSDF on Glass Fac=0.8 → KEEP_GLASS=3; other Mix Facs 0.1–0.6 not 0/1). 0 PRUNE_ALPHA/VOLUME/AOV. Classroom/loft still 0. Auto off, no time claim.
- Opaque cutout shadows off on proven CLIP/HASHED cutouts only.
- Sample knee: already-adaptive files pad an extra doubling (512→128 becomes 256). Adaptive-off interiors stay one doubling (300→128).
- CAMERA_CULL tags linked scatter/tiny (Cycles flags, not hide_render) and turns on Simplify with high caps. Distance cull stays off.
- N-panel is one Make it Fast button plus Revert. Analyze / VRAM / Manual / Tune / Safety stay closed. Auto click runs Analyze then the speed stack, then Fit-to-Budget only if VRAM is over. Draft / Quantize / Tune are not auto-fired.
- CAMERA_CULL does not skip objects shared with other local scenes; Cycles evaluates the flag against the rendering camera (Classroom dustParticules/volumeLight).
- used-outside ignores linked library scenes, so CAMERA_CULL can tag linked scatter that only lives in this file.
- last_report maxlen is 1 MB and writes never emit truncated JSON, so Analyze grade survives Make it Fast.
- Operator copy reports the padded sample count (128 / 256), not the raw probe knee.
- FILTER_GLOSSY: `blur_glossy` 0→1.0 when glossy/glass/anisotropic/clearcoat is proven. Never raises a user value already > 0. GROUP/HERO-only skipped.
- AUTO_SCRAMBLE: `auto_scrambling_distance` on for GPU, paired with TABULATED_SOBOL. Never writes a huge manual `scrambling_distance`.
- Public Classroom claim stays 41%. loft unchanged.

**Versioning rule (tied to the journal schema):** a release that changes the journal
schema in a way older versions cannot faithfully revert is a **major** bump; additive,
revert-compatible changes (new levers, new entry fields with safe defaults) are a
**minor** bump; anything with no journal schema change is a **patch**.
Every shipped zip bumps `blender_manifest.toml` and is named `scenequant-X.Y.Z.zip`. Never replace the same version number.

## 0.3.2 — 2026-08-19

- Persistent Data on stills (next F12 keeps the BVH).
- Transparent shadows cap 4 when the scene proves alpha/glass.

## 0.3.1 — 2026-08-19

- Manifest website points at the public repo: `github.com/liminallyspaced/ScreenQuantDev`.
- Same Make it Fast addon as 0.3.0.

## 0.3.0 — 2026-08-19

First public build of **Make it Fast**.

- One-click revertible Cycles speed plan (adaptive sampling, sample-knee cap, OIDN / GPU denoise, bounce/clamp, light tree, caustics, camera cull, dead geometry).
- Draft / Fast GI / resolution tricks stay out of the default click.
- Also ships Analyze, Fit to Budget (VRAM), Probe Sample Knee, Verify Render, Revert All.

Measured on RTX 2080 Super Max-Q 8 GB, factory-startup, persistent data off:

- Classroom (Seux), Blender 4.5.5 LTS: 2:43 → 1:35 (41%)
- loft.blend, Blender 4.5.5 LTS: 5:37 → 2:41 (52%)
- loft.blend, Blender 5.1.2: 5:50 → 2:12 (62%)
